/**
 * MathUtils
 * - shorcuts to often used Math functions
 * 
 * TODO:
 * - merge with another more powerful library such as Numeric.js
 */

var d2r = Math.PI/180,
    PI = Math.PI,
    cos = Math.cos, 
    sin = Math.sin, 
    abs = Math.abs, 
    log10 = Math.log10, 
    sqrt = Math.sqrt,
    atan2 = Math.atan2,
    asin = Math.asin,
    floor = Math.floor,
    pow = Math.pow;